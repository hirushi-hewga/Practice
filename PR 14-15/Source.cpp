#include <iostream>
using namespace std;



int main()
{
	/*
	int x = 2;
	double H;
	if (x > 1)
		H = 2 * exp(3 * x);
	else
		H = abs(x + 1);
	cout << "H = " << H << endl;


	x = -2;
	if (x > 1)
		H = 2 * exp(3 * x);
	else
		H = abs(x + 1);
	cout << "H = " << H << endl;
	*/
}